# Kopiera till .env och fyll i värden:
#   cp .env.example .env
#
# OBLIGATORISKT för inloggning (annars får du serverfel vid Logga in):
JWT_SECRET=byt_till_en_lång_hemlig_nyckel_minst_16_tecken
#
# Använd fil-DB (Otto, Lärare, föräldrar i mock-db.json) – sätt till 1:
USE_FILE_DB=1
#
# Om USE_FILE_DB=1 används INTE MongoDB – allt läses från server/mock-db.json.
# Lämna MONGO_URL kommenterad när du använder fil:
# MONGO_URL=mongodb://localhost:27017
# MONGO_DB_NAME=MindGrow
#
# PORT för API (standard 4000):
# PORT=4000
